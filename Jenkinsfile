pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                echo 'Сборка проекта...'
            }
        }

        stage('Test') {
            steps {
                echo 'Запуск тестов...'
            }
        }

        stage('Deploy') {
            steps {
                echo 'Деплой приложения...'
            }
        }
    }
}
